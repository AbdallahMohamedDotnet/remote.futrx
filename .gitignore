# Build artifacts
/backend/remote
/backend/public/

# Frontend deps + build outputs
/frontend/node_modules/
/frontend/dist/
/frontend/.vite/
/frontend/tsconfig.tsbuildinfo

# Runtime state — don't commit chat history or local data
/data/
/.qa.env

# OS / editor noise
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/

# Logs
*.log

# Marketing + docs sites: deps and build outputs
/remote.futrx.com/node_modules/
/remote.futrx.com/dist/
/docs.remote.futrx.com/node_modules/
/docs.remote.futrx.com/dist/

# Claude Code runtime artifacts
/.claude/scheduled_tasks.lock

# remote.futrx.com is its own git repository — track it separately
# (or wire it as a proper submodule), never as an embedded gitlink.
/remote.futrx.com/
