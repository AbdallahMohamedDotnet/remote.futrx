# remote.futrx — canonical runtime configuration.
#
# Rendered by infra/install.sh. The systemd unit and /usr/local/bin/remote
# both source this file so they cannot disagree about BASE_URL, DATA_DIR, or
# INSTALL_DIR. Repair a damaged copy with a full infra/install.sh (or
# infra/update.sh) convergence rather than editing it by hand.
BASE_URL=https://${HOSTNAME}
DATA_DIR=${INSTALL_DIR}/data
INSTALL_DIR=${INSTALL_DIR}
